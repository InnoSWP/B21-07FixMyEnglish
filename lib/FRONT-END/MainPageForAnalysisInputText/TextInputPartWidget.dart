import 'package:flutter/material.dart';

class TextInputPartWidget extends StatelessWidget {
  const TextInputPartWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(width: 386, color: const Color(0xFFE9F1E8), child: Column(children: [],));
  }
}
