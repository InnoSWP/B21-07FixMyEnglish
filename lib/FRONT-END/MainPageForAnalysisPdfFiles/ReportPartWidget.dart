import 'package:flutter/material.dart';

class ReportPartWidget extends StatelessWidget {
  List<String> list;
  ReportPartWidget(this.list, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(children: [],);
  }
}
